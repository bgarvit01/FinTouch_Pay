<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.aflak.fingerprintdialoglibrary">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".finalactivity"></activity>
        <activity android:name=".midactivity" />
        <activity android:name=".firstpage" />
        <activity
            android:name=".Homepagefinal"
            android:label="@string/title_activity_homepagefinal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage" />
        <activity android:name=".Menu" />
        <activity android:name=".SimpleDialog" />
        <activity android:name=".SecureDialog" />
        <activity android:name=".SimpleView" />
        <activity android:name=".thirdpage" />
        <activity android:name=".SecureView" />
    </application>

</manifest>